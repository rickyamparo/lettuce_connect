<!DOCTYPE html>
<html>
  <head>
    <title>LettuceConnect</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/css/bootstrap.min.css" integrity="sha384-PsH8R72JQ3SOdhVi3uxftmaW6Vc51MKb0q5P2rRUpPvrszuE4W1povHYgTpBfshb" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= stylesheet_link_tag    'root', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-center">
      <ul class="nav justify-content-center text-center">
        <li class="nav-item">
        <div class="nav-link"><%= link_to "Home", root_path%></div>
        </li>
      <% if current_user %>
          <li class="nav-item">
          <div class="nav-link"><%= link_to "Logout", logout_path, method: :delete %></div>
          </li>
          <li class="nav-item">
            <div class="nav-link"><%= link_to "Connections", connections_path %></div>
          </li>
          <li class="nav-item">
            <div class="nav-link"><%= link_to "Add Handle", new_user_handle_path(current_user) %></div>
          </li>
          <li class="nav-item">
            <div class="nav-link"><%= link_to "Manage Handles", user_handles_path(current_user) %></div>
          </li>
        <% else %>
        <li class="nav-item">
          <%= link_to "Login", login_path %>
        </li>
        <li class="nav-item">
          <%= link_to "Register", new_user_path %>
        </li>
        <% end %>
      </ul>
  </nav>
  <% flash.each do |type, message| %>
        <div class="green-flash">
          <%= message %>
        </div>
          <% end %>
    <%= yield %>
  </div>
  </body>
</html>
